<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>


	<artifactId>chufala-common</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>chufala-common</name>
	<description>chufala-common</description>
	<packaging>jar</packaging>

	<parent>
		<groupId>com.hope.chufala</groupId>
		<artifactId>chufala</artifactId>
		<version>0.0.1-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>

	<properties>
		<java.version>17</java.version>
	</properties>

	<!-- 说明：本模块是库（jar），没有 main 类，因此不引入
	     spring-boot-maven-plugin —— 它的 repackage 目标会因找不到主类而失败，
	     导致整个项目的 mvn package 挂掉。可执行的重打包只由 chufala-web 负责。 -->

</project>
