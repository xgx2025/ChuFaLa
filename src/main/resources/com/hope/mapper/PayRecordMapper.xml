<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hope.mapper.PayRecordMapper">
    <select id="selectByOrderId" resultType="com.hope.domain.entity.PayRecord">
        select * from pay_record  where order_id = #{orderId}
    </select>

</mapper>